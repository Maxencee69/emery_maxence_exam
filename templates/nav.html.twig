<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a href="{{ path('app_index') }}" class="navbar-brand" style="display: flex; align-items: center;" onclick="window.location.href = '{{ path('app_index') }}';">
            <svg width="40" height="40" xmlns="http://www.w3.org/2000/svg" style="margin-right: 10px;">
                <rect x="2" y="8" width="36" height="24" rx="5" ry="5" fill="#028a6a" />
                <circle cx="20" cy="20" r="10" fill="white" />
                <circle cx="20" cy="20" r="6" fill="#028a6a" />
                <rect x="15" y="4" width="10" height="4" fill="#028a6a" />
                <rect x="8" y="16" width="4" height="2" fill="white" />
            </svg>
            Filmcameras
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-camera">
                    <a class="nav-link active" aria-current="page" href="{{ path('appareils_photo_list') }}">Appareils photo</a>
                </li>
                <li class="nav-camera nav-camera-large-gap">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_brand') }}">Recherche par marques</a>
                </li>
                <li class="nav-camera nav-camera-large-gap">
                    <a class="nav-link" href="{{ path('camera_new') }}">Ajouter un appareil sur Filmcameras</a>
                </li>
            </ul>
        </div>
        <div class="d-flex align-items-center">
            {% if app.user %}
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="{{ path('admin_cameras') }}" class="btn btn-outline-secondary me-2">
                        <i class="bi bi-gear"></i> Admin
                    </a>
                {% else %}
                    <a href="{{ path('user_profile') }}" class="btn btn-outline-secondary me-2">
                        <i class="bi bi-person-circle"></i> Mon profil
                    </a>
                {% endif %}
                <a href="{{ path('app_logout') }}" class="btn btn-outline-secondary">
                    <i class="bi bi-box-arrow-right"></i> Se déconnecter
                </a>
            {% else %}
                <div class="btn-group">
                    <button type="button" class="btn btn-outline-secondary dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle"></i> Se connecter
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><a class="dropdown-item" href="{{ path('app_login') }}">Se connecter</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_register') }}">S'inscrire</a></li>
                    </ul>
                </div>
            {% endif %}
        </div>
    </div>
</nav>
