{# templates/camera/item.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}
  {{ parent() }} - Appareil photo
{% endblock %}

{% block body %}

<img src="{{ camera.photoPath }}" class="card-img-top" alt="{{ camera.modelName }}">
<div class="card-body">
    <h5 class="card-title">{{ camera.modelName }}</h5>
    <a href="{{ path('camera_item', {'id': camera.id}) }}" class="btn btn-primary">Voir les détails</a>
</div>

{% endblock %}
